 if (curr_proc() != NULL) {
                    mycpu()->inkernel_trap--;  // Temporarily decrease to allow yield
                    yield();
                    mycpu()->inkernel_trap++;  // Restore after yield
                }
